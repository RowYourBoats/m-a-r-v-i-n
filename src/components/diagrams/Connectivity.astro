---
// Side-by-side comparison: all-to-all vs. nearest-neighbor connectivity.
// The geometry + animation live in connectivity.client.ts (shared with inline
// markdown-doc embeds); this shell is just the mount placeholder. Styling is in
// global.css (.diagram*), so it applies equally here and on project pages.
---

<div class="diagram-mount" data-diagram="connectivity"></div>

<script>
  import { mountAll } from "./registry";
  mountAll();
</script>
